/** Which build is running (commit and build date), for bug reports. */
declare const __APP_BUILD__: string;
/** Built by `npm run phone`: reports back to the PC serving it (src/testlog.ts). */
declare const __TEST_LOG__: boolean;
